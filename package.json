{
  "name": "family-dashboard-tooling",
  "version": "1.0.0",
  "private": true,
  "description": "Dev-only tooling for shared/ — never deployed. web/ and api/ are self-contained packages.",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "@types/luxon": "^3.7.3",
    "@types/node": "^26.1.2",
    "luxon": "^3.7.2",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10",
    "zod": "^4.4.3"
  }
}
